Question no.7: 7. Extract the first three characters from the `student_name` column in the `Students` table.

Answer: mysql> select substr(student_name, 1, 3) as first_three_chars
    -> from students;